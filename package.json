{
  "name": "kiemke-lau2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd kiem-ke-app && npm install && npm run build && node -e \"const fs=require('fs'); fs.cpSync('dist', '..', {recursive: true});\"",
    "dev": "cd kiem-ke-app && npm run dev",
    "start": "node server.js"
  }
}
