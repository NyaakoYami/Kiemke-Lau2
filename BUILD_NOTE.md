# Build note

The redesigned source is ready.

```bash
npm install
npm run dev
```

For production:

```bash
npm run build
npm run preview
```

The project keeps the existing Supabase sync and JSON import/export configuration.
